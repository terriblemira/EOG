# minecraft control
import asyncio
import websockets
from collections import deque
import eog_reader
import time
import pyautogui

# queue = deque()

# async def minecraft_connection(websocket):
#     print(f"Connected")

# async def main():
#     async with websockets.serve(minecraft_connection, "localhost", 3000):
#         print("Ready, server started on ws://localhost:3000")
#         await asyncio.Future()  # run forever

# asyncio.run(main()) 

